{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Retro Dev Build",
			"type": "shell",
			"command": "docker run --rm -v $PWD:/m68k -t registry.gitlab.com/doragasu/docker-sgdk:v1.90",
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"group": {
				"kind": "build",
				"isDefault": true
			}
		},
		{
			"label": "Retro Dev Run",
			"type": "shell",
			"command": "retrodevhelper",
			"args": [
				"run",
			],
			"options": {
				"cwd": "${workspaceFolder}"
			}
		}
	]
}